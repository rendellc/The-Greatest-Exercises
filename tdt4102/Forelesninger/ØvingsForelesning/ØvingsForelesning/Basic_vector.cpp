#include <vector>
#include <iostream>
#include <algorithm>
#include "basic_vector.h"
using namespace std;
/*
int basic_vector()
{
	vector<double> verdier = { 0.0, 22.2, 33.3, 100.0 };
	verdier.push_back(44.4);
	verdier.insert(verdier.begin(), 55.5);
	verdier.insert(verdier.end(), 66.6);

	verdier.erase(verdier.begin()+2);
	sort(verdier.begin(), verdier.end());

	for (int i = 0; i < verdier.size(); i++) {
		cout << verdier[i] << "\t";
	}

	return 0;
}
*/